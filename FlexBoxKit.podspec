Pod::Spec.new do |s|

  s.name             = "FlexBoxKit"
  s.version          = "1.0.2"
  s.platform         = :ios
  s.source_files     = "FlexBoxKit/*.{h,m,c}", "NXTSegmentedControl/*.{h,m}"
  s.requires_arc     = true

end
